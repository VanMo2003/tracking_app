import 'package:flutter/material.dart';

class NotificationScreent extends StatelessWidget {
  const NotificationScreent({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text("Thông báo"),
        centerTitle: true,
      ),
    );
  }
}
